import { ScrollView, View } from "react-native";
import TurnoScreen from "../screen/TurnoScreen";

export default function Turno() {
  return (
    <ScrollView>
      <View>
        <TurnoScreen />
      </View>
    </ScrollView>
  );
}
